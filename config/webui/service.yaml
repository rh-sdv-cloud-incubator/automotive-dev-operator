apiVersion: v1
kind: Service
metadata:
  name: automotive-dev-webui
  namespace: automotive-dev-operator-system
  labels:
    app.kubernetes.io/name: automotive-dev-webui
    app.kubernetes.io/part-of: automotive-dev-operator
    app.kubernetes.io/component: webui
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: automotive-dev-webui