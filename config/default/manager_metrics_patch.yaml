# This patch was generated by kubebuilder but our manager already sets metrics args.
# Keep this file present but make it a no-op to avoid kustomize patch errors.
- op: test
  path: /kind
  value: Deployment
